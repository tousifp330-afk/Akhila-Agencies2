import'dotenv/config';export const config={supabase:{url:process.env.SUPABASE_URL!,serviceRoleKey:process.env.SUPABASE_SERVICE_ROLE_KEY!},tally:{host:process.env.TALLY_HOST||'localhost',port:parseInt(process.env.TALLY_PORT||'9000',10),companyName:process.env.TALLY_COMPANY_NAME||''}};export function validateConfig():string|null{if(!config.supabase.url)return'SUPABASE_URL is required';if(!config.supabase.serviceRoleKey)return'SUPABASE_SERVICE_ROLE_KEY is required';if(!config.tally.companyName)return'TALLY_COMPANY_NAME is required';return null;}
