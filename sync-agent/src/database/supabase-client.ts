import{createClient,SupabaseClient}from'@supabase/supabase-js';import{config}from'../config.js';let client:SupabaseClient|null=null;export function getSupabaseClient():SupabaseClient{if(!client){client=createClient(config.supabase.url,config.supabase.serviceRoleKey,{auth:{persistSession:false}});}return client;}
export async function bulkUpsert(table:string,rows:Record<string,unknown>[],conflictColumn:string):Promise<{count:number;error:string|null}>{const supabase=getSupabaseClient();const bs=500;let inserted=0;for(let i=0;i<rows.length;i+=bs){const batch=rows.slice(i,i+bs);const{error}=await supabase.from(table).upsert(batch,{onConflict:conflictColumn,ignoreDuplicates:false});if(error)return{count:inserted,error:`Batch ${i}: ${error.message}`};inserted+=batch.length;}return{count:inserted,error:null};}
export async function getCompanyId(name:string):Promise<string|null>{const{data}=await getSupabaseClient().from('companies').select('id').eq('name',name).single();return data?.id||null;}
export async function getSyncState(companyId:string){const{data}=await getSupabaseClient().from('sync_state').select('*').eq('company_id',companyId).maybeSingle();return data||{company_id:companyId,initial_sync_completed:false,current_sync_status:'idle'};}
export async function updateSyncState(companyId:string,updates:Record<string,unknown>){const s=getSupabaseClient();const{data:existing}=await s.from('sync_state').select('id').eq('company_id',companyId).maybeSingle();if(existing)return s.from('sync_state').update(updates).eq('company_id',companyId);else return s.from('sync_state').insert({company_id:companyId,...updates});}
export async function recordSyncRun(companyId:string,run:{mode:string;status:string;started_at?:string;completed_at?:string;date_range_start?:string;date_range_end?:string;voucher_count?:number;ledger_count?:number;stock_count?:number;customer_count?:number;supplier_count?:number;bank_count?:number;cash_count?:number;error_message?:string}){return getSupabaseClient().from('sync_runs').insert({company_id:companyId,...run});}
