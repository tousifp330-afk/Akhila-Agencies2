import{XMLParser}from'fast-xml-parser';const parserOptions={ignoreAttributes:false,attributeNamePrefix:'@_',isArray:(name:string,jpath:string)=>{const ap=['LEDGER','LEDGERENTRIES.LIST','LEDGERENTRIES.LEDGERENTRY.LIST','STOCKITEM','STOCKITEM.LIST','VOUCHER','VOUCHER.LIST','ALLLEDGERENTRIES.LIST','LEDGERENTRIES.LIST','INVENTORYENTRIES.LIST','BILLALLOCATIONS.LIST','TAXENTRIES.LIST'];return ap.some(p=>jpath.endsWith(p));},textNodeName:'#text',trimValues:true,parseAttributeValue:true};const parser=new XMLParser(parserOptions);export function parseTallyXml(xml:string):Record<string,unknown>{return parser.parse(xml);}export function safeText(node:unknown,def=''):string{if(!node)return def;if(typeof node==='string')return node;if(typeof node==='object'&&node!==null){const o=node as Record<string,unknown>;if('#text'in o)return String(o['#text']??def);}return def;}export function safeNumber(node:unknown,def=0):number{const t=safeText(node,'');if(!t)return def;const n=parseFloat(t);return isNaN(n)?def:n;}export function safeGuid(node:unknown):string{return safeText(node,'').replace(/[{}]/g,'').trim();}
