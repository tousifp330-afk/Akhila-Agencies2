import{safeText,safeNumber,safeGuid}from'./xml-parser.js';export interface ParsedLedger{tally_guid:string;master_id:string;name:string;parent_group:string;parent_group_id:string;opening_balance:number;closing_balance:number;balance_type:'Dr'|'Cr';classification:string;address:string;gstin:string;state_name:string;pin_code:string;contact_person:string;phone:string;email:string;pan:string;}export function parseLedgers(xmlData:Record<string,unknown>):ParsedLedger[]{const ledgers:ParsedLedger[]=[];try{const e=xmlData['ENVELOPE']as Record<string,unknown>|undefined;const b=e?.['BODY']as Record<string,unknown>|undefined;const id=b?.['IMPORTDATA']as Record<string,unknown>|undefined;const rd=id?.['REQUESTDATA']as Record<string,unknown>|undefined;const tm=rd?.['TALLYMESSAGE']as Record<string,unknown>|undefined;const lm=tm as Record<string,unknown>|undefined;let ll:unknown[]=[];if(lm?.['LEDGER']&&Array.isArray(lm['LEDGER'])){ll=lm['LEDGER']as unknown[];}else if(Array.isArray(lm?.['LEDGER']?.['LIST'])){ll=(lm!.LEDGER as Record<string,unknown>).LIST as unknown[];}for(const item of ll){const l=item as Record<string,unknown>;if(!l||!(l['LEDGERNAME']||l['NAME']))continue;const pg=safeText(l['PARENT']);let cls='general';if(pg==='Sundry Debtors')cls='customer';else if(pg==='Sundry Creditors')cls='supplier';else if(pg==='Bank Accounts')cls='bank';else if(pg==='Cash-in-Hand')cls='cash';const cb=safeNumber(l['CLOSINGBALANCE']);ledgers.push({tally_guid:safeGuid(l['GUID']),master_id:safeText(l['MASTERID']),name:safeText(l['LEDGERNAME']||l['NAME']),parent_group:pg,parent_group_id:safeText(l['PARENTID']),opening_balance:safeNumber(l['OPENINGBALANCE']),closing_balance:cb,balance_type:cb>=0?'Dr':'Cr',classification:cls,address:safeText(l['ADDRESS']),gstin:safeText(l['GSTIN']||l['INCOMETAXNUMBER']),state_name:safeText(l['STATENAME']),pin_code:safeText(l['PINCODE']),contact_person:safeText(l['CONTACTPERSON']),phone:safeText(l['PHONE']||l['PHONENUMBER']),email:safeText(l['EMAIL']),pan:safeText(l['PAN']||l['PANNO'])});}}catch(e){console.error('[LedgerParser] Error:',e);}return ledgers;}
