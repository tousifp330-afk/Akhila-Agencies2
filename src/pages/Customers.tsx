import React, { useEffect, useState } from 'react';import { useCompany } from '../lib/company';import { getCustomers } from '../lib/api';import { navigate } from '../lib/router';import { LoadingWrapper, SearchBar, Pagination } from '../components/ui';import { formatCurrency } from '../lib/utils';import type { Ledger } from '../lib/types';

export function CustomersPage() {const { company } = useCompany();const [customers, setCustomers] = useState<Ledger[]>([]);const [search, setSearch] = useState('');const [loading, setLoading] = useState(true);const [error, setError] = useState<string|null>(null);const [page, setPage] = useState(1);const pageSize=50;
useEffect(()=>{if(!company)return;setLoading(true);getCustomers(company.id,search).then(({data,error:err})=>{if(err)setError(err);else setCustomers(data||[]);setLoading(false);setPage(1);});},[company,search]);const paginated=customers.slice((page-1)*pageSize,page*pageSize);
return(<LoadingWrapper loading={loading} error={error}><div className="erp-page-header"><h1 className="erp-page-title">Customers (Sundry Debtors)</h1><SearchBar value={search} onChange={setSearch} placeholder="Search customers..." /></div><div className="erp-card"><div className="erp-card-body !p-0"><table className="erp-table"><thead><tr><th>Customer Name</th><th className="num">Closing Balance</th><th>Group</th></tr></thead><tbody>{paginated.length===0&&<tr><td colSpan={3} className="text-center text-slate-400 py-8">No customers found</td></tr>}{paginated.map(c=>(<tr key={c.id}><td className="clickable" onClick={()=>navigate(`/ledger/${c.id}`)}>{c.name}</td><td className="num amount-positive">{formatCurrency(Math.abs(c.closing_balance))}</td><td className="text-slate-500">{c.parent_group}</td></tr>))}</tbody></table></div></div><Pagination page={page} pageSize={pageSize} totalCount={customers.length} onPageChange={setPage} /></LoadingWrapper>);}
